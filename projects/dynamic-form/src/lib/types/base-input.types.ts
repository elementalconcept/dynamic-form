// These are only a suggestion.
export const baseInputTypes = [
  'text', // textarea,
  'string', // input[type=text][inputmode=text]
  'password', // input[type=password]
  'number', // input[type=text][inputmode=numeric]
  'email', // input[type=text][inputmode=email]
  'tel', // input[type=text][inputmode=tel]
  'url', // input[type=text][inputmode=url]
  'radio', // input[type=radio]
  'checkbox', // input[type=checkbox]
  'file', // input[type=file]
  'date',
  'time',
  'datetime',
  'select', // select
  'multiselect', // select[multiple]
  '_description_' // Non input element
];
